name: Prebuild bom

on:
  workflow_dispatch:
  # pull_request:
  #   branches:
  #     - main
  #     - "**"
  #   paths:
  #     - src/bom/**
  # pull_request_target:
  #   paths:
  #     - src/bom/**
  # push:
  #   paths:
  #     - src/bom/**
  pull_request:
    types:
      - opened
      - edited
    paths:
      - src/bom/**

jobs:
  prebuild-bom:
    runs-on: ubuntu-latest
    steps:
      - name: Flag prebuilds as done
        run: echo "prebuilds are done"
